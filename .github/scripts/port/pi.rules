# Pi: the question tool is the vendored stage_questionnaire, sub-agents go through
# stage_subagent, the shell is bash and the reader is read.
AskUserQuestion	`stage_questionnaire`
`Agent`	`stage_subagent`
\bBash\b	`bash`
`Read`	`read`
CLAUDE\.md	AGENTS.md
CLAUDE	AGENTS
, `model: sonnet`	
、`model: sonnet`	
