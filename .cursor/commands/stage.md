# `/stage` — route a request to the right STAGE skill

Read `.agents/commands/stage.md` and apply its router to whatever the user typed beside `/stage` in this message.

For an unmarked skill, read the Cursor-owned copy under `.cursor/skills/` in full and follow it. No text beside the command selects `stage-flow-status` with no argument.
