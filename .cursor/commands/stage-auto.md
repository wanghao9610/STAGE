# `/stage-auto` — pursue a stated paper goal

Read `.agents/commands/stage-auto.md` and follow it with whatever the user typed beside `/stage-auto` in this message.

For an unmarked skill, read the Cursor-owned copy under `.cursor/skills/` in full and follow it. A skill marked † is never started: print the exact `/stage-<name> <argument>` command as the run's closing line and stop, as the shared file says. No text beside the command means no goal: ask for one.
