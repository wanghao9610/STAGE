# Agent 指令

> 对照原文：[AGENTS.md](AGENTS.md)。本译文只供人阅读；英文原文件始终是运行时权威版本。

这些行为准则用于减少大语言模型常见的写作错误。它们偏向谨慎而非速度；对于简单任务，请自行判断。这是一个 STAGE 仓库：一个仓库对应一篇论文，手稿里的每个数字都能追溯到证据，或被明确标为缺失。

## 1. 写之前先思考

**不要臆断。不要掩饰困惑。明确说明权衡。**

- 明确陈述假设。目标有歧义时——哪一节、哪张图、哪条论断、哪个周期——停下来，列出候选项并询问。绝不要猜用户指的是哪一个。
- 一个结果存在多种解读时，把它们都列出来——不要默默选一个。
- 证据支撑不了句子想声称的那么多时，写之前先说明。必要时提出异议。

## 2. 先有证据，再写文字

**手稿中的任何内容都不能伪装成有来源。**

- `manus/` 中的每个数字，要么追溯到本次运行读取过的一条带指纹 `mates/` 记录，要么写成 `\todo{...}`——没有第三种状态。显眼的 todo 是一种状态；看似合理但编造出来的数字是缺陷。
- 关于被引论文的每项断言，都能对照 `notes/refs/` 中的阅读笔记，或 `mates/` 下导入的参考资料核验。
- Venue 规则——页数上限、截止日期、检查单——只以用户确认的事实录入。Venue 的 LaTeX class 同样是一项事实：它来自用户提供的官方模板包，逐字节复制，绝不自行下载，也绝不凭记忆写出。
- 证据不得就地修改。数字错误时，在源头修正后重新导入，或登记一份修正后的文件；绝不要“改正”现有证据。
- 不得为了显得有帮助而削弱这些规则。它们正是为截止日期压力而校准的。
- 起草与润色遵循 `docs/mds/stage-workflow/human-writing-guide.md`：保留经作者确认的声音，依据模式组合而不是孤立词语诊断公式化表达，绝不能仅为了显得像人而增加事实或个性。

## 3. 精准修改

**只改必须改的内容。只说一次，直截了当。**

- 不要“改进”相邻的正文、图注或格式；不要重写没有问题的内容。即使你会采用不同写法，也要匹配手稿现有的声音。
- 绝不要顺手重排章节编号：`<n>_` 前缀同时绑定提纲行、记录表的 `Stated in` 列，以及 `main.tex` 中的 `\input` 顺序（规约 §5.5）。
- 用能表达观点的最少文字：不写填充语，不为凑页数而扩写，也不再用第二句话重复第一句。
- 产出表与它追踪的正文一起移动——claim 行、outline 行、notation 条目——在同一次改动中完成，而不是事后清理。

检验标准：每一处改动都能追溯到用户的请求，其中的每个数字都能追溯到一个指纹。

## 4. 以目标驱动执行

**定义成功标准。反复推进直至验证完成。**

- 把任务转化为可运行的检查：“起草实验章节”→ 构建通过，且每个数字都带 `% src:` 锚点或 `\todo`；“压到八页”→ `lint.sh` 报告页数不超过当前周期的 `page_limit_main`；“回复评审”→ 记录表中的每个问题都有回复或承诺。
- 对多步骤任务，说明各步骤以及关闭每一步的检查。

## 5. 写作工作流

**本项目使用 STAGE 写作工作流。它的记录存在文件中，而非聊天历史中。**

- 证据只读地存放在 `mates/`，其指纹登记于 `mates/MANIFEST.md`；每条论断的陈述、证据和状态都是 `notes/claims.md` 中的一行。
- 不清楚当前进展时，先运行 `/stage-flow-status`——它读取提纲、记录表、manifest 与周期状态，并指出唯一的下一步行动。
- 每个工作流 skill 遵循的规则位于 `docs/mds/stage-workflow/writing-workflow-conventions.md`；每次运行开始时完整读取。Skill 名册在其 §11，各 skill 的职责写在 `writing-workflow-skills.md`。
- 每次 skill 运行只在该 skill 的提交步骤提交一次，其他时候不提交（规约 §1）。
- 不要手工编辑 `wkdrs/` 下生成的报告，也不要编辑 `docs/mds/stage-workflow/`、skill 目录树或 `execs/` 下的任何脚本——包括两个入口和 `execs/scpts/` 下的三个工具；`execs/update.sh` 会覆盖它们，也会覆盖本文件。

## 6. 回复语言

**`.env` 中的 `STAGE_LANG` 决定聊天回复和一次运行写出的 Markdown 的语言**——`notes/`、`tasks/`、模拟评审、`wkdrs/` 报告。完整规则见规约 §7.6。

- 已设置（`en` 或 `zh`）→ 无论对话语言是什么都使用该语言。未设置或为空 → 跟随用户的对话语言；对话中的明确要求优先于两者。每次运行只解析一次：`grep -sE '^STAGE_LANG=' .env || true`。
- 无论其取值是什么，以下内容始终用英文：`manus/` 下的一切——正文、图注、`% src:` 注释、`\todo{}` 文本——以及 `cycls/<cycle>/response/` 下给评审的回复。二者都会被仓库之外的人阅读。
- 任何语言的文档中，结构性字面量都保持英文：frontmatter 的键和值、记录表状态、claim 与 point ID、路径、bibkey、venue、dataset 与 metric 名称——也就是脚本会 grep 的一切。
- 已有文档保持它原本的语言；`STAGE_LANG` 只管一次运行写出的内容，从不触发重翻译。`*.zh-CN.md` 文档和 `SKILL_zh.md` 文件只供人阅读，不会在运行时装载，也从不是权威版本。

## 7. 回复措辞

**用任何语言都应写明动作，而不是只写动作名称。读者不应需要解码术语才知道你做了什么。**

- 必须出现的名称要在同一句中附带其含义。
- 例外：需要逐字匹配的字符串——记录表状态、ID、字段名、路径——保持原样。在旁边解释，不要用解释替换原文。
- 本身不说明内容的指针——`§9`、`C4`、`R2.W1`、某个周期名——保持原样，并用几个词说明其指向的内容，例如 `C4（零样本迁移论断）`。每次回复首次使用时都要说明，而不是每次对话只说明一次；读者不会向上翻找。
- 收束一次执行的回复，要以下一条要执行的命令结尾，精确、可直接运行——`/stage-*` 调用或 shell 命令行，逐字给出——或者明说已无可执行之事。读者永远不该问"我现在敲哪条命令？"（规约 §7.5）。
- 使用技术性表达，不写填充语，不使用 emoji。朴素不等于闲聊。

## 8. 项目布局

**把文件放在指定目录中。**完整表格与规则见规约 §10。

- 手稿放在 `manus/`：入口为 `main.tex`；章节为 `secs/<n>_<slug>.tex`；图放在 `figs/`，源文件放在 `figs/srcs/`；表放在 `tabs/`；参考文献为 `bibs/reference.bib`；模板层放在 `stys/`。
- 手稿始终以预印本形式编译，venue 格式是生成的副本：官方模板包整包解压到 `cycls/<cycle>/template/`，绝不放在 `manus/` 下，后者会被 `lint.sh` 扫描（规约 §10.4）。
- 证据放在 `mates/`，且只读。`execs/scpts/import.sh` 与 `/stage-evid-curator` 是仅有的写入者，并且只做带指纹的整文件新增或替换。
- 写作元数据放在 `notes/`：`story.md`、`claims.md`、`outline.md`、`notation.md`、`style.md`、`adopt.md`，阅读笔记放在 `notes/refs/`。
- 投稿周期放在 `cycls/<venue>_<year>/`。修订暂存、承诺清单与 venue 跟进项放在 `tasks/`：`<cycle>_promises.md` 会阻塞 camera-ready，直到每个复选框都兑现；`<cycle>_venue.md` 不阻塞任何流程。
- 构建产物与临时报告放在 `wkdrs/`——被 Git 忽略且可重新生成。可留存的结论写进记录表和 `tasks/`，而不是报告文件。
- `execs/` 根目录封闭：只放 `run.sh` 与 `update.sh`。工具脚本（`import.sh`、`lint.sh`、`fmt.sh`）放在 `execs/scpts/`。

## 9. 项目运行时

**使用项目入口。不要猜测本机路径。**

- `bash execs/run.sh` 是唯一构建方式：由 latexmk 在树外的 `wkdrs/builds/` 中构建，engine 取自 `.env` 的 `LATEX_ENGINE`。它打印 PDF 路径与页数。
- `bash execs/scpts/lint.sh` 是确定性关卡：检查未定义引用、`\todo` 数量、相对当前周期 `venue.yml` 的页数，以及 `ANON=true` 时的身份泄漏。硬失败以非零状态退出。
- `bash execs/scpts/fmt.sh` 让 `manus/` 保持每句一行。LaTeX 把换行当作空格，所以每句单独一行不会占用 PDF 空间，却能让 `% src:` 注释与它管的句子形成固定的两行对。新正文按这种方式书写，不要按列宽换行；`--check` 报告偏差，`lint.sh` 会给出警告。它从不修改 `manus/stys/` 或 `cycls/*/template/` 下的 venue 模板包，并会拒绝任何可能改变排版文本的重写。
- 运行时配置放在由 `.env.example` 创建的 `.env` 中：`STAR_HOME`（空表示独立使用）、`LATEX_ENGINE`、`ANON`、`STAGE_REPOSITORY`、`STAGE_HARNESSES`、`INVOLVE`、`STAGE_LANG`。不要硬编码本机路径。
- 只写真实日期：每个写进产物的日期都来自系统时钟，绝不凭记忆或臆造（规约 §4）。

## 10. 项目记忆

**一次会话学到的内容写进项目的 `.stage/memory/`，而不是你自己的记忆存储。**

- 只有当仓库里没有其他文件认领某项事实时才记录——数字属于 `mates/`，论断属于 `notes/claims.md`，页数上限属于对应周期的 `venue.yml`，被引论文说了什么属于 `notes/refs/`，承诺属于 `tasks/`。记忆只保存剩余信息。
- 记忆不是来源：它绝不能为 `manus/` 中的数字、venue 规则或关于被引论文的断言提供依据（无论记忆写了什么，§2 始终有效）。
- 只提议，不擅自写入：每次会话最多提议两次，并且只在用户同意后写入。`INVOLVE=low` 会不经询问记录，但必须说明。
- 只对本机成立的事实写进 Git 忽略的 `.stage/memory/local/`。记忆与仓库文件冲突时，以仓库文件为准。
- 类型、文件格式、钩子解析的索引行，以及记忆的退役方式见 `docs/mds/stage-workflow/memory_spec.md`。

## 11. 验证

**在声称完成之前，证明改动有效。**

- 修改 `manus/` 下任何内容后都要构建；没有编译过的正文不算完成。改动可能影响引用、todo 数量或页数时，运行 `lint.sh`。
- 本次运行中重新读取你引用的每个数字所在的 `mates/` 文件。记得某个指纹不等于核验过它。
- 无法运行某项检查时，说明原因并指出剩余风险。
- 报告已验证的内容及其证据——PDF 路径与页数、lint 结论、发生状态变化的记录表行——不能只说“可用”。
