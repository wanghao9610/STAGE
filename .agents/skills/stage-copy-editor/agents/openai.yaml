interface:
  display_name: "Copy Editor"
  short_description: "Polish prose without changing meaning, numbers, or citations"
  default_prompt: "Use $stage-copy-editor to polish this section's prose without touching any fact or number."
policy:
  allow_implicit_invocation: true
