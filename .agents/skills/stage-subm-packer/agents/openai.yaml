interface:
  display_name: "Submission Packer"
  short_description: "Preflight, package, record, and freeze a submission"
  default_prompt: "Use $stage-subm-packer to preflight and package this submission, then freeze the record."
policy:
  allow_implicit_invocation: false
