interface:
  display_name: "Poster Builder"
  short_description: "Select, render, and check the active conference poster"
  default_prompt: "Use $stage-pstr-builder to plan what reaches the poster, then render and check it; the sheet size is asked and recorded in the poster plan, and any official poster kit is the one the user supplies."
policy:
  allow_implicit_invocation: false
