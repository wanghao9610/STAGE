interface:
  display_name: "Response Writer"
  short_description: "Turn reviews into a point ledger, response, and promises"
  default_prompt: "Use $stage-resp-writer to turn these reviews into a point-by-point response and a promise list."
policy:
  allow_implicit_invocation: false
